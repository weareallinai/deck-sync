'use client';

import { useEffect } from 'react';

export function PreloadManager() {
  // Stub: will preload next slide assets
  useEffect(() => {
    // Preload logic will go here
  }, []);

  return null; // This is a utility component with no UI
}

